version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r data-harvester/requirements.txt -t data-harvester

artifacts:
  base-directory: data-harvester
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
