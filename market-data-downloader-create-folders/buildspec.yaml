version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r market-data-downloader-create-folders/requirements.txt -t market-data-downloader-create-folders

artifacts:
  base-directory: market-data-downloader-create-folders
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
