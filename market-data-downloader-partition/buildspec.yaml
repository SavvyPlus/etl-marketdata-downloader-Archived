version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r market-data-downloader-partition/requirements.txt -t market-data-downloader-partition

artifacts:
  base-directory: market-data-downloader-partition
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
